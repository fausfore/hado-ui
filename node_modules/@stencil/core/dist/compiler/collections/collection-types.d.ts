import { BuildCtx, CompilerCtx, Config, PackageJsonData } from '../../declarations';
export declare function generateTypes(config: Config, compilerCtx: CompilerCtx, buildCtx: BuildCtx, pkgData: PackageJsonData): Promise<void>;
