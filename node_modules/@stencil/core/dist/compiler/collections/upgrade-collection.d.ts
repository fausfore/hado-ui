import { BuildCtx, Collection, CompilerCtx, Config } from '../../declarations';
export declare function upgradeCollection(config: Config, compilerCtx: CompilerCtx, buildCtx: BuildCtx, collection: Collection): Promise<void>;
